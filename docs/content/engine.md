# Engine architecture

*Stub — to be written. See task #7. Will document: VM channels/threads,
the ~256-entry game-variable bank with known indices, the opcode set,
the video buffer model, and the resource type semantics
(`SOUND` / `MUSIC` / `POLY_ANIM` / `PALETTE` / `BYTECODE` /
`POLY_CINEMATIC` / `UNKNOWN`).*

This page is the source for the canonical names recorded in
`symbols/_base.yaml`.
